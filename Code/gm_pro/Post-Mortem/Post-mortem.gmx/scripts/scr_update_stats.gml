///scr_update_stats
// Dynamic Stats
maxHp = hp;
cutOffHp = hp * 10;

maxStamina = stamina;
cutOffStamina = hp * 10;

cutOfStrength = strength * 50;

cutOffSpd = spd * 2;

cutOffCriticalProbability = 90;
