/// scr_stat_create(health, defence, attack, speed)
var map = ds_map_create();
map[? "health"] = argument0;
map[? "defence"] = argument1;
map[? "attack"] = argument2;
map[? "speed"] = argument3;
return map;
