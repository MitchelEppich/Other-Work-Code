#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

#include "Save.h"

void Teach::Save(std::string type, std::string group, std::string header, std::string word)
{
	
}

void Teach::Locate(std::string id, int type)
{
	//std::string format;
	//
	//switch (type)
	//{
	//case 0: format = ftype; break;
	//case 1: format = fgroup; break;
	//case 2: format = fheader; break;
	//case 3: format = fcomponent; break;
	//case 4: format = fitem; break;
	//}

	//std::string unique_identity = format;
	//int point_index = unique_identity.find("-!-");
	//transform(id.begin(), id.end(), id.begin(), (int(*)(int))tolower);
	//unique_identity.replace(point_index, point_index + 3, id);

	//std::string start = unique_identity;
	//start.replace(point_index - 1, 1, "");
	//std::string end = unique_identity;

	//std::fstream file("language.txt");
	//std::ifstream file;

	//file.open("language.txt", std::ios::in);

	//int start_index;
	//int end_index;
	//point_index = 0;

	//while (file.good())
	//{
	//	std::string item;
	//	getline(file, item);

	//	if (item == start) start_index = point_index;
	//	if (item == end) end_index = point_index;

	//	point_index++;
	//}
}

void Teach::Index(std::string item, )

void Teach::Insert(std::string item, int type)
{

}